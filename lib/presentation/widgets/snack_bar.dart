import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class FuelSnackBar extends StatelessWidget {
  const FuelSnackBar({super.key});

  @override
  Widget build(BuildContext context) {
    return SnackBar(
      content: Placeholder(),
    );
  }
}
